@layout AccountLayout
@page "/account/ResetPassword/{Code}"

<EditForm Model="@ResetPasswordCommand" OnValidSubmit="ResetUserPassword">
    <EditContextApiExceptionFallback @ref="EditContextApiExceptionFallback" />
    <FluentValidationValidator />
    <MudText Typo="Typo.h4" GutterBottom="true">@Resource.Reset_Password</MudText>
    <MudText>@Resource.Reset_your_password</MudText>

    <MudTextField Label="@Resource.Email"
                  Variant="Variant.Outlined"
                  @bind-Value="ResetPasswordCommand.Email"
                  For="@(() => ResetPasswordCommand.Email)" />

    <MudTextField Label="@Resource.Password"
                  Variant="Variant.Outlined"
                  @bind-Value="ResetPasswordCommand.Password"
                  For="@(() => ResetPasswordCommand.Password)"
                  InputType="@PasswordInput"
                  Adornment="Adornment.End"
                  AdornmentIcon="@PasswordInputIcon"
                  OnAdornmentClick="TogglePasswordVisibility" />

    <MudTextField Label="@Resource.Confirm_Password"
                  Variant="Variant.Outlined"
                  @bind-Value="ResetPasswordCommand.ConfirmPassword"
                  For="@(() => ResetPasswordCommand.ConfirmPassword)"
                  InputType="@PasswordInput"
                  Adornment="Adornment.End"
                  AdornmentIcon="@PasswordInputIcon"
                  OnAdornmentClick="TogglePasswordVisibility" />
    <MudDivider />
    <MudButton ButtonType="ButtonType.Submit"
               Style="margin-top: 4px"
               Variant="Variant.Filled"
               Color="Color.Primary"
               Size="Size.Large"
               FullWidth="true">
        @Resource.Submit
    </MudButton>
    <MudExpansionPanels>
        <MudExpansionPanel Text="@Resource.Validation_Summary" IsExpanded="true" Disabled="true" HideIcon="true">
            <MudText Color="@Color.Error">
                <ValidationSummary />
            </MudText>
        </MudExpansionPanel>
       </MudExpansionPanels>
</EditForm>

trigger:
  branches:
    include:
      - develop  # Change this if your default branch is different

pool:
  name: Self-Hosted

steps:
  - checkout: self
    persistCredentials: true

  - script: |
      git config --global user.email "davewevans72@gmail.com"
      git config --global user.name "davewevans"
      git remote add github https://davewevans:$(GITHUB_PAT)@github.com/davewevans/Elevate-OTT-API-CMS.git
      git push --mirror github
    displayName: 'Mirror to GitHub'




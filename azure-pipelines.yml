trigger:
  branches:
    include:
      - develop  # Change this if your default branch is different

pool:
  vmImage: 'ubuntu-latest'

steps:
  - checkout: self
    persistCredentials: true

  - script: |
      git config --global user.email "davewevans72@gmail.com"
      git config --global user.name "Azure DevOps Bot"
      git remote add github https://davewevans:${{ secrets.GITHUB_PAT }}@github.com/davewevans/Elevate-OTT-API-CMS.git
      git push --mirror github
    displayName: 'Mirror to GitHub'



